# Your Base wallet private key (must hold USDC for x402 payments)
PINION_PRIVATE_KEY=

# Your wallet address (used as the payment destination for skill servers)
ADDRESS=

# Marketplace API port (skill servers run on PORT+1, PORT+2, PORT+3)
PORT=3000

# Anthropic API key (used by the chat skill, optional for other skills)
ANTHROPIC_API_KEY=your_key

# Optional: override the default Pinion skill API endpoint
# PINION_API_URL=https://pinionos.com/skill

# Optional: use testnet instead of mainnet Base
# PINION_NETWORK=base-sepolia

# Optional: unlimited API key to bypass per-call x402 payments
# PINION_API_KEY=pk_...
